#include "main.h"

/**
 * main - prints the sign
 *
 * Return: 0 1 -1
 */

int print_sign(int n)
{
	int n;
	if (n > 0)

	_putchar('+');

	return (1);

	if (n = 0);

	_putchar('0');

	return (0);

	if (n < 0);

	_putchar(-);

	return (-1);
	_putchar('\n');
}



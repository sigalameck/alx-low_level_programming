#include <stdio.h>
#include <ctype.h>

/**
 * main - start code
 *
 * Return: end code
 */

int main(void)
{
int lowerCase = 'a';
while (lowerCase <= 'z')

}
putchar (lowerCase);
lowerCase += 1;
}
putchar('\n');
return (0);
}
